import React from 'react';
import {Text} from 'react-native';
import MainWrapper from '../../components/wrapper/wrapper';

const BusHomeScreen = props => {
  return (
    // <MainWrapper>
    <Text> Home screen</Text>
    // </MainWrapper>
  );
};

export default BusHomeScreen;
